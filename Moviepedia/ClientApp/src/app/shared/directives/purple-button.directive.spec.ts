import { PurpleButtonDirective } from './purple-button.directive';

describe('PurpleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PurpleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
